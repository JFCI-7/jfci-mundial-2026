{
  "buildCommand": "pnpm install && node build.mjs",
  "outputDirectory": ".",
  "functions": {
    "api/proxy.js": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/api/proxy/(.*)", "destination": "/api/proxy" }
  ]
}
